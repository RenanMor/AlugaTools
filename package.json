{
  "name": "alugatools-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k \"pnpm --prefix backend dev\" \"pnpm --prefix frontend dev\"",
    "build": "pnpm --prefix backend build && pnpm --prefix frontend build",
    "check": "pnpm --prefix backend check && pnpm --prefix frontend check"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^7.0.3"
  },
  "packageManager": "pnpm@9.12.0"
}